import { mount } from '@vue/test-utils'
import Checkbox from '@/components/Checkbox.vue'

describe('Checkbox.vue', () => {
  it('emit events when input on checkbox', () => {
    const wrapper = mount(Checkbox, {
      propsData: {
        //
      }
    })
  })
})
